# 💡 Core Concepts

## Collections

A **Collection** is a logical grouping of agent memories (documents, thoughts, etc).

## Memory Entries

Each entry stores content, type, importance, and metadata.

## Embeddings

AgentVectorDB supports pluggable embedding functions for semantic search.

> [!info]
> You can use any embedding model compatible with your agent framework.